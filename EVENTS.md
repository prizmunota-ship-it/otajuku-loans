# 年間イベントと出欠の管理

2026-09-13から、予定データの正本は `events.json`。`schedule.html` は表示用。
日付・名称・詳細・会場・時間・出欠項目はJSON内の同じイベントで管理する。
`id` は保存キーに使うので、開催名や時間を変更しても変えない。

- `startDate` / `endDate`: 西暦の日付。`endAt` は回答の受付終了（日本時間のオフセットを含める）。
- `sessions`: 参加・不参加を別々に回答する項目。見学会と打ち上げは2項目。
- `zoom`: 詳細画面内の入室リンク。
- `attendanceUrl`: 既存の外部回答を残すイベントだけ指定。この場合は新しい出欠保存を受け付けない。
- 詳細へのリンク: `/schedule#event=<id>`。

金融機関祭りVol.3は、本人の「今回は移行せず残す」という指示で従来の調整さんを維持。8名分の既存回答は移行していない。新しい入力との二重運用を避け、詳細画面から従来の回答先へ案内する。

## 保存

`functions/api/event-attendance.js` が既存の `ZAIKO_KV` を使う。
キーは `event-rsvp:v1:<eventId>:<SHA-256(lowercase email)>`。他アプリのキーには触れない。
参加者1名につき1キーなので、複数人の回答を一括JSON上書きして失うことがない。
通信・照合・保存エラー時は保存成功にしない。

APIは毎回既存GASで在籍メールを照合し、localStorageの認証フラグだけでは受け付けない。
ただし、これは従来ポータルと同じ**メール文字列の名簿照合**であり、メール所有者の本人認証ではない。
管理者権限や機微情報の閲覧をこの照合だけで追加しない。返信一覧にメールは返さない。

KVの反映には最大1分程度の遅れがある。保存応答はその回答を返して画面に即時反映する。
90秒間の保存控えをsessionStorageに置き、更新直後の古い読込で巻き戻さない。
`clear` は自分のキーへ取消し状態を書き込み、古い値や未反映値を読んで削除を空振りしない。

## 確認と公開

`tests/event-attendance.test.mjs` をNodeで実行。メンバー別の保存、変更、取消し、受付終了、拒否・通信失敗、外部回答の保全を検証する。
画面は既存と同じBabel standalone 7.26.4を維持。mainへのpush後、公開版でイベントタップと詳細画面を確認する。
本番確認用の一時回答は、実際の回答と区別した名前で作り、必ず取り消しまで確認する。
